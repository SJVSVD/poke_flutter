import 'package:flutter/material.dart';

class AcompanamientoTab extends StatelessWidget {
  const AcompanamientoTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pokebolas', style: TextStyle(fontSize: 20)),
    );
  }
}
