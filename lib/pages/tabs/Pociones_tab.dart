import 'package:flutter/material.dart';

class BebidaTab extends StatelessWidget {
  const BebidaTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Pociones',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
