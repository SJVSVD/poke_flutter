import 'package:flutter/material.dart';

class ComidaTab extends StatelessWidget {
  const ComidaTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pokemones'
      , style: TextStyle(fontSize: 20)),
    );
  }
}
